# Node.js Express Visitor Logger Backend

This backend receives visitor data from your website and logs it to a file.

## Features
- POST /log endpoint to receive JSON data
- Logs all received data to logs.json

## Usage
1. Start the server: `node index.js`
2. Send POST requests with visitor data to `http://localhost:3000/log`

---

You can now proceed to scaffold the backend project.